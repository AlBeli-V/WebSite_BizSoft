# Разбор логики отчёта конкурентной разведки за 10.09.2026

Что разбиралось: `ci-c98370a0ebe87d97/latest.html` (он же
`reports/competitive/archive/2026-09-10.html` ветки `competitive-data`) и
письмо того же прогона. Сетевой доступ к проду из сессии закрыт, поэтому
документ читался из ветки-хранилища — побайтово тот же файл, что отдаёт nginx.

Каждое утверждение ниже проверено по коду и по артефактам дня
(`data/competitive/processed/2026-09-10-*.json`,
`data/competitive/snapshots/2026-09-10-discovery.json`), а не по тексту
отчёта. Там, где отчёт объясняет сам себя, объяснение проверялось отдельно:
в двух случаях оно оказалось неверным.

## С чем согласен

Это не вежливость: перечисленное стоит защитить от правок, потому что
именно оно отличает контур от генератора предложений.

1. **Отказ переводить потенциал в переходы.** Спрос по показам Вебмастера —
   видимая нам часть спроса за две недели, а не объём рынка. Отчёт говорит
   это прямо в каждом пакете вместо того, чтобы выдать красивое число.
2. **Три разных запрета вместо одного «занято».** Свой мораторий на замере,
   чужой эксперимент базового контура, контрольная группа чужого
   эксперимента — разные состояния с разными последствиями. Модель верна.
3. **Вынос «Проверить, а не делать» из очереди поручений.** Пакет, где
   правок по репозиторию не требуется, — не работа, и место настоящей
   работы он занимать не должен.
4. **Контрольная группа при оценке эффекта** плюс исключение страниц,
   задетых правкой шаблона, плюс фиксация состава контроля по обоим окнам.
   Это ровно те три способа получить ноль на живом эффекте, и все три сняты.
5. **Названная граница вместо додуманного числа.** Разложить расхождение с
   Вебмастером на вклад рекламы и вклад ранжирования нечем, потому что все
   три доступных источника видят только органику. Сказано прямо.
6. **Google и Яндекс не складываются.** Доли считаются внутри своего поля,
   сравнивается присутствие, а не позиции.
7. **Отказ рекомендовать ссылки и поведенческие факторы.** Контур их не
   измеряет — значит и предлагать под них бюджет нельзя.

## Изъяны логики

По убыванию тяжести. Тяжесть меряется одним: приводит ли изъян к неверному
действию, к неверному числу перед руководителем или только к неудобству.

### 1. План работ выдаёт 22 поручения, а взять можно 3

Самый дорогой изъян, потому что он ведёт к действию, которое ломает два
замера сразу.

Раздел 4 объявляет «План работ — 22 пакетов» и расписывает каждый до
чек-листа и приёмки, включая шаг «отправить страницу на переобход». Из этих
22 пакетов:

| состояние | пакетов | до какого срока |
| --- | --- | --- |
| занята чужим экспериментом базового контура | 16 | 26.09 – 01.10 |
| контрольная группа чужого эксперимента | 3 | 06.10 |
| действительно свободна | 3 | — |

Свободны только WP-31, WP-35, WP-39. Их суммарный индекс потенциала 0.0058
из 0.2369 по всем 22 — **2,4% объявленного**.

Письмо, которое руководитель читает вместо отчёта, ставит поручением дня
WP-01 (`/blog/kak-oplatit-framer-dlya-yurlica`) — страницу, занятую
экспериментом `content-6-clusters` до 30.09. Раздел «ВАРИАНТЫ ДЕЙСТВИЙ»
предлагает «Полный охват: все 22 пакетов». В письме пометки занятости нет
вообще; она есть только в разделе 5 веб-отчёта, строкой в таблице на 186
строк.

Причина — одна строка. `competitive-intelligence/run_daily.py:304`:

```python
_, occupied = occupancy_mod.mark(packages, today=date)
```

`mark()` возвращает `(свободные, занятые)` — готовое разделение. Вызов
берёт только вторую половину и выбрасывает первую; `packages` остаётся
нетронутым. Занятость проставляется пометкой и никого никуда не выводит.

Это расходится с собственной документацией модуля
(`attack_engine/occupancy.py`, шапка): «ЗАНЯТА — Это запрет: пакет выводится
из очереди до контрольной точки». Соседние два фильтра того же файла
(`on_watch`, `to_verify`) сделаны правильно — списком, а не пометкой.

### 2. Threat в веб-отчёте посчитан в режиме, который данные не разрешают

`reports/deep_report.py:409` вызывает `threat_mod.rank()` без аргумента
`core_stable`, у которого по умолчанию `True`. `run_daily.py:177` тот же
`rank()` вызывает с настоящим значением, а сегодня оно `False`: ядро выросло
с 431 до 489 запросов.

Результат — два разных ответа из одного прогона:

* письмо: «Следим: raketapay.ru — Threat 36 **из 70**»;
* отчёт: raketapay.ru, Threat 36, «**полный режим (шкала 0–100)**: падение
  доли между окнами −0.96 п.п.».

Динамика по разным ядрам — ровно то, что подвал того же отчёта запрещает:
«сравнивать цифры этого дня с другими днями допустимо только при совпадении
… отпечатка ядра». Флаг `core_stable` и существует, чтобы этого не делать.

Побочное следствие: таблица угрозы ранжирует вперемешку строки шкалы 0–70 и
0–100 под подписью «числа разных режимов между собой несравнимы». Отчёт
запрещает сравнение и тут же сортирует по нему единый список. С исправленным
вызовом все строки станут базовыми, и разнобой исчезнет сам.

### 3. «+0 переходов» — ноль на месте отсутствия данных

Раздел 4: «суммарная оценка по пакетам с сопоставимым спросом: **+0
переходов** при выходе в ТОП-3». `deep_report.py:1237` складывает
`p['traffic_upside'] or 0` по 22 пакетам, а `traffic_upside` у всех
двадцати двух `None`.

Методика того же отчёта: «Отсутствие данных нигде не показывается как ноль».
Это и правило `report-integrity`. Здесь оно нарушено в самой заметной строке
раздела.

Вдобавок два пакета дня, у которых upside измерен (1.2 и 0.4), в этот срез
не попали — они в `on_watch` и `to_verify`. То есть ноль стоит там, где
измеренное значение существует, просто отфильтровано.

### 4. «Вне плана работ — 10»: причина названа неверно

Раздел 5, таблица состояний: «Вне плана работ — 10 — запрос не сведён в
пакет: спрос не измерен либо страница не в нашей зоне».

Проверка по артефакту: все 10 сведены в пакеты. Это 7 запросов со статусом
«не поручение — проверка» (WP-12, WP-21, WP-30, WP-38, WP-40, WP-44) и 3 со
статусом «снято с поручений» (WP-04). Причина в таблице ложна для всех
десяти.

Механизм: `_attack_summary` (`deep_report.py:~505`) разбирает статус по
префиксам и ловит четыре из шести; «снято с поручений» и «не поручение —
проверка» падают в `else`, а `else` подписан «вне плана».

Тут же, на том же экране, заголовок таблицы: «Полная таблица точек атаки —
186 строк, **из них в плане работ 186**». Две строки одного раздела
противоречат друг другу. `planned` в `_strike_table` уже починен
(комментарий на `deep_report.py:1167` описывает эту самую починку) — счётчик
состояний остался от прежней версии.

### 5. «В очереди на работу — 15» включает 6 запросов, которые брать нельзя

Статус контрольной группы начинается со слов «в очереди, но кластер —
контроль эксперимента …», и разбор по префиксу засчитывает его в очередь.
Правка контрольной группы губит чужой эксперимент так же надёжно, как
правка его страницы. Очередь — 9 запросов, а не 15.

### 6. Сверка с Вебмастером сравнивает непересекающиеся периоды

Срез — 10.09. Окно Вебмастера — 08.08 – 04.09. Окна не пересекаются вовсе:
между концом окна и срезом 6 дней. Всё, что позиции сделали за эти шесть
дней, записано в «расхождение» и объяснено рекламой и ранжированием.

`decision_engine/position_check.py` аккуратно исключает запросы со
страницами, созданными после окна (`baseline_queries`), — то есть код умеет
брать срез на конец окна, — но применяет это только к отбору запросов, а
сравнивает всё равно с сегодняшним срезом.

Отдельно вывод «сдвиг неравномерен — значит дело не только в рекламе». Вывод
верный: рекламные блоки сдвинули бы все позиции примерно одинаково. Но
названа не та альтернатива, точнее — не названа вовсе. Наблюдаемый профиль
(+3.05 на первой позиции, +3.28 на 2–3, −0.09 на 6–10) — это подпись
регрессии к среднему: позиция 1 в срезе может отклоняться в среднем только
вниз, позиция 8 — в обе стороны. Сравнение одного дня со средней за месяц
даёт такой профиль само по себе, даже при нулевой рекламе. Пока это не
исключено, размер расхождения интерпретировать нельзя.

Это же нарушает правило `report-integrity`: «Периодные сравнения — только по
полным окнам».

### 7. Главная находка дня не превращена ни в одно поручение

216 запросов из 375: мы в топ-10 Яндекса, в Google в топ-20 нас нет. Отчёт
сам называет это «главным вопросом по Google» и перечисляет три гипотезы —
индексация, авторитет домена, конкурентоспособность страницы. Дальше не
происходит ничего: ни ранжирования гипотез, ни проверки, которая их разводит,
ни строки в плане работ.

Между тем гипотезы неравноценны. Отсутствие в топ-20 по 58% общего ядра при
первых местах в Яндексе — это профиль проблемы индексации, а не
ранжирования: слабая страница занимает 30–50 место, а не отсутствует. И
проверяется она дешевле остальных двух.

Это не дефект кода — это незакрытая петля: контур нашёл самое дорогое и
оставил находку без поручения.

### 8. «639 запросов» в шапке против «ядро 489» в подвале

Шапка: «639 запросов». Плитки: «В ТОП-3 87 **из 639**», «В ТОП-10 276 из
639». Доля видимости 5,8% — тоже по полю 639
(`наши_показатели.запросов_в_поле = 639`).

Подвал: «ядро запросов v10 (7ad54bcfefb4, **489 запросов**) · покрытие по
запросам 1.0 · источники спроса {webmaster: 400, wordstat: 38, none: 51}» —
сумма ровно 489. Блок `по_запросам`, по которому считается сравнимая доля,
тоже 489 строк.

То есть измеряется поле из 639 запросов, а сравнимость гарантируется хешем
объекта из 489, и «покрытие 1.0» относится к 489. Разница в 150 запросов
нигде не объяснена. Числа не ошибочны, но читаются как относящиеся к одному
объекту, а относятся к двум.

### 9. Мелкие противоречия текста

* «Предложено 22 · на наблюдении 23 · оценено 0» читается воронкой, но 22 —
  пакеты сегодняшнего дня, а 23 — эксперименты за все дни. Разные единицы в
  одной строке.
* Письмо: «B2B Share Яндекс 5,8% (к 09.09 +0,63 п.п.)» и через строку
  «Сравнение с 2026-09-09 не приводится: ядро выросло с 431 до 489». Обе
  фразы верны про разные величины (дельта по пересечению двух дней и прямая
  разность долей), но стоят рядом и читаются как противоречие.

## Чего в отчёте нет, а должно быть

102 точки атаки из 186 и 16 пакетов из 22 заблокированы экспериментами
базового SEO-контура до 26.09 – 01.10. Это не случайность дня: два контура
делят один и тот же набор страниц, и разведка на три недели остаётся без
фронта работ. В отчёте этот факт не сформулирован ни разу — он выводится
только сложением строк таблицы на 186 строк. Это управленческий вывод дня, и
он должен стоять в итогах, а не собираться читателем.

## Что уже сделано

П1 и П2 закрыты в версии методики 1.9.1 (см. раздел «Приоритеты» ниже —
нумерация пунктов совпадает):

| пункт | что изменилось | где |
| --- | --- | --- |
| 1 | `mark()` отдаёт оба списка, занятые выходят из очереди; в отчёте и письме появился раздел «Заблокировано чужим замером» со сроком освобождения | `run_daily.py`, `deep_report.py`, `mailer/` |
| 2 | `core_stable` передаётся в `rank()` веб-отчёта и стал обязательным позиционным аргументом `_leaderboard` — пропуск теперь TypeError, а не тихая ошибка | `deep_report.py` |
| 3 | `_upside_total()` печатает отказ считать переходы вместо «+0» | `deep_report.py` |
| 4 | «снято с поручений» и «проверка, а не поручение» получили свои строки счётчика; подпись таблицы согласована | `deep_report.py` |
| 5 | контрольные группы вынесены из «в очереди» в свою строку | `deep_report.py` |

Сверх плана добавлены две вещи, вскрывшиеся при правке:

* **Проверка на выходе.** `occupancy.split_takeable()` отсеивает занятый
  пакет в самих отчёте и письме, независимо от того, правильно ли собран
  вызов. Изъян 4 был недочиненным остатком прежней такой же починки — здесь
  инвариант держится в точке, где документ собирается.
* **Условие едет вместе с поручением.** Контрольная группа остаётся в
  очереди намеренно (это условие, а не запрет), но ограничение теперь стоит
  и в executive-строке письма, а не только в карточке ниже. Иначе поручение
  ушло бы в работу молча: после вывода занятых из очереди верхним пакетом
  дня 10.09 становится ровно контрольная группа.

Проверка на данных дня: раздел «План работ» — 6 пакетов вместо 22, ещё 16 в
разделе «Заблокировано»; сумма состояний точек атаки 186 при 186 точках,
строка «вне плана работ» — ноль; режим Threat базовый и совпадает с письмом.
Тестов в контуре стало 344; шестнадцать новых собраны в
`tests/test_report_integrity_2026_09_10.py` и проверены на падение при
прежнем коде.

### П3 закрыт в 1.9.2

**Сверка с Вебмастером считается по пересекающимся периодам.** Наша сторона —
медиана позиции по всем срезам внутри окна выгрузки, а не срез дня прогона.
Правка сняла не одну ошибку, а две: непересекающиеся окна и перекос от
сравнения точки со средней.

Замер до и после на данных 10.09:

| | точка против средней | медиана против средней |
| --- | --- | --- |
| медиана расхождения | +2.63 | +1.25 |
| позиция 1 | +3.05 | +2.25 |
| позиции 2–3 | +3.28 | +2.07 |
| позиции 6–10 | −0.09 | +0.44 |
| сопоставлено запросов | 77 | 163 |

Профиль по диапазонам выпрямился — это и была подпись регрессии к среднему,
которую прежняя версия приписывала рекламе. Остаточная неравномерность
(+2.25 наверху против +0.44 в середине) теперь настоящая, и вывод «дело не
только в рекламе» впервые опирается на измерение, а не на артефакт. Выборка
выросла вдвое: запрос больше не обязан попасть именно в сегодняшний срез.

Следствие, которое нельзя было не тронуть: **базовая линия 1.8 измерена
прежним, смещённым способом.** Она пересчитана на 1.25 с явной записью в
конфиге, что значения разных методик несравнимы. Оставить прежнюю значило бы
поднимать тревогу позже, чем нужно: порог стоял бы на 0.55 позиции выше
реального уровня.

**Поле и ядро разведены.** В шапке теперь сказано, что доли и счётчики
ТОП-3/ТОП-10 считаются по полю (639 запросов), а сравнимость с другими днями
— по ядру (489). Раньше первое стояло в шапке, второе в подвале, и связи
между ними не было нигде.

**Обоснование убрано под кат.** Почему сравниваются именно эти периоды, почему
медиана, а не один срез, и что осталось ограничением — раскрывается нажатием.
Вывод виден всегда, доказательство не занимает экран.

### П4 закрыт в 1.9.3

Отчёт называл разрыв с Google главным вопросом, перечислял три гипотезы и не
проверял ни одной. Оказалось, что данные для их разведения уже собраны — в том
же срезе, за который уже заплачено.

**Что измерено.** Google-срез прочитан целиком: все адреса нашего сайта,
встреченные по любому запросу на любой позиции. Затем взяты наши страницы,
которые в Яндексе держат топ-10 по запросам разрыва.

| | значение |
| --- | --- |
| наших страниц в разрыве | 64 |
| из них встречаются в Google-срезе хоть где-то | 1 |
| не встречаются ни разу | 63 |
| наших адресов во всём Google-срезе | 1 |

**Почему это разводит гипотезы.** Три объяснения дают разные наблюдаемые
следствия. Слабая конкурентоспособность страницы означает, что страница в
выдаче есть, но ниже: она попадает в собранную глубину хотя бы по части
запросов. Недостаток авторитета домена действует так же — ровнее и ниже, но
присутствие сохраняется. И только отсутствие страницы в видимой выдаче даёт
ноль вхождений сразу по всем запросам. Наблюдается третье, причём среди
отсутствующих есть страницы, держащие в Яндексе первое место по 35 запросам.

**Что теперь выдаёт отчёт.** Вывод «проверять первой надо видимость страниц, а
не их качество», список страниц по весу (сколько запросов держит каждая) и
поручение с приёмкой: проверить индексацию в Search Console, записать по
каждой ответ. Вывод выбирается измерением, а не заранее: при обратной картине
блок говорит «страницы Google видит, вопрос в ранжировании», при смешанной —
отказывается сводить к одной гипотезе.

**Почему это порядок, а не диагноз.** Отсутствие в срезе не доказывает
отсутствия в индексе: страница может быть проиндексирована и стоять
пятидесятой. Поэтому вывод назначает очерёдность проверок. Она и есть главное:
проверка индексации бесплатна и занимает минуты, а доработка текста под Google
— недели работы редактора. Начинать со второго значит платить за диагноз самую
высокую цену из возможных.

Обоснование целиком убрано под кат.

## Второй круг аудита: что нашлось после правок

Три дефекта, которых не было видно, пока первые пять закрывали обзор.

### 6. Компонент весом 20 из 100 ни разу не был измерен

В каждой строке таблицы угрозы каждый день стояла пометка «приближение по
ТОП-3: счётчик „выше нас“ не передан». Так и было: аргумент не передавал ни
один вызов, и компонент «превосходство над нами» всегда подменялся числом
ТОП-3. Данные для точного счёта лежали в том же срезе — позиция наша и
позиция конкурента есть по каждому запросу.

Приближение не просто занижало оценку, оно прятало конкурента определённого
типа. Число ТОП-3 отвечает на вопрос «часто ли он берёт первые места», а
компонент спрашивает другое: «по какой доле поля он стоит выше нас».

| домен | ТОП-3 | выше нас | приближением | измерением |
| --- | --- | --- | --- | --- |
| aifory.pro | 4 | 67 | 4 | 6 |
| migsoft.ru | 13 | 32 | 4 | 4 |

Приближение делает тихого конкурента неотличимым от втрое более слабого.
Величина теперь считается в реестре из того же среза и едет в карточке
домена, а не отдельным аргументом: величина, которую надо не забыть
передать, рано или поздно забывается.

### 7. Утверждение о методе, которое сам метод опровергает

Отчёт писал: «Недоступные факторы не заменяются средним: их вес
пропорционально распределяется между измеренными». Вторая половина верна,
первая — нет, и проверяется подстановкой в формулу. Пропорциональное
перераспределение веса **тождественно** подстановке недоступному фактору
взвешенного среднего измеренных, до последнего знака.

Формулировка писалась против прежнего поведения и для него была верна: до
версии 1.1.0 неизвестный спрос давал 0,5 — общую середину шкалы, одинаковую
для всех целей. Нынешний метод подставляет среднее самой цели по её же
измеренным факторам, и это заметно честнее. Но подстановкой быть не
перестаёт.

Смещение направленное, а не случайное: у цели, сильной по всем измеренным
факторам, недоступная уязвимость страницы конкурента считается такой же
сильной. Именно этого утверждать нельзя — сильный коммерческий интент
запроса ничего не говорит о том, легко ли отобрать позицию у конкретного
конкурента. Бьёт по верхушке списка, там, где принимаются решения. Метод не
менялся: он защитим. Изменилось то, как он назван.

### 8. Штраф за отсутствие не соответствовал глубине выдачи

Отчёт писал «отсутствие в ТОП-20 считается позицией 21». Срез Яндекса
собирается глубиной 10 — все 639 строк содержат ровно десять результатов.
Отсутствие означает «хуже десятого», а штраф ставился как за двадцать
первое.

Это не косметика. Эффект правки считается по изменению медианной позиции, и
при таком штрафе пересечение границы топ-10 перевешивало всё остальное:
уход с 9-го места на 11-е выглядел падением на 12 позиций вместо двух.
Эксперименты ведутся на страницах, стоящих на 4–10 местах, то есть ровно у
этой границы. Ни один опыт ещё не доведён до оценки, поэтому исправление
ничего не ломает задним числом.

### 9. Письмо спорило само с собой о сравнимости

Строка показателей давала «к 09.09 +0,63 п.п.», а главный сигнал строкой
ниже сообщал: «Сравнение с 2026-09-09 не приводится, доли по разным ядрам
несопоставимы». Обе фразы верны и говорят о разных величинах: первая — о
дельте по пересечению составов ядра, вторая — о прямом сравнении долей.
Рядом они читаются как спор.

Теперь сигнал отказывает именно в прямом сравнении и указывает, где стоит
сравнимая величина.

### Ошибка в моей собственной проверке

Отдельным пунктом, потому что она попала в ветку. Я сочла, что тест
платёжной проверки падает на main, и починила его. Проверка была ошибочной:
локальная копия main отставала на один коммит, и в ней фикса ещё не было. На
настоящем main тест уже починен тем же способом. Свою версию убрала слиянием
в пользу main.

## Третий круг: дефект, который прятался за остальными

### 10. Динамический вердикт был недостижим по построению

Светофор письма каждый день сообщал: «Недостаточно данных для оценки
динамики: нужно 6 сравнимых измерений, не хватает 6». Читается как
«подождите ещё немного». Ждать было бесполезно.

Ряд строился по **одной общей корзине** запросов на все дни истории. Ядро
растёт почти ежедневно, поэтому общее пересечение по построению только
сжимается.

| основание | пересечение | годится |
| --- | --- | --- |
| вся история (11 снимков) | 59 из 489 (12%) | нет |
| последние 6 дней | 139 из 489 (28%) | нет |
| соседние дни, попарно | 86–100% | да |

Чем дольше работает контур, тем недостижимее становился вердикт. Добивали
общее пересечение два дня неполного сбора: 05.09 и 06.09 дали 209 и 224
запроса против четырёхсот, и одни они обрезали корзину всем остальным дням.

Соседние дни при этом сравнимы отлично. Отсюда сцепление — стандартный приём
для индексов с меняющейся корзиной: доля пересчитывается на пересечении
каждой пары, берётся её изменение, изменения складываются в ряд. Пара с
пересечением ниже порога рвёт цепь, берётся последний непрерывный отрезок.

На данных 10.09 строгий ряд пуст, сцепленный даёт 4 измерения с 07.09.
Вердикт говорит «есть 4, не хватает 2» — и эти два дня действительно
наступят. Строгий ряд остаётся предпочтительным: когда состав ядра
устоится, контур вернётся к нему сам. Основание названо и в вердикте, и в
подвале отчёта.

## Четвёртый круг: генератор поручений

### 11. Полоса кандидата обещала охват, которого не было

Отчёт печатал «Кандидат — запрос, где мы на 4–20 позиции». Срез Яндекса
собирается глубиной 10: из 489 строк все содержат ровно десять результатов, и
позиций 11–20 в данных нет ни одной. Запрос, где мы одиннадцатые, попадал не
в «есть куда расти», а в «нас нет в выдаче вовсе» — то есть выпадал из работы
как безнадёжный, будучи ближайшим кандидатом.

Полоса теперь считается из глубины среза, а не из константы, и отчёт печатает
фактическую. Тот же урок, что со штрафом за отсутствие: величина, выведенная
из данных, не расходится с ними.

### 12. Одно слово получало два голоса

Маркеры интента — подстроки, и часть вложена друг в друга: «цен» целиком лежит
внутри «лицензи». Насыщение задумано как «два разных признака», а считалось
как «два совпадения», поэтому «лицензия adobe» получала полный коммерческий
интент 1.0 — больше, чем «тариф recraft» с его 0.7.

На ядре это меняло оценку у 5 запросов из 489, то есть дефект мелкий. Правка
сделана общей, а не вырезанием одного маркера: список маркеров живой, и
следующее вложение появилось бы незаметно. Заодно убран мёртвый B2B-маркер
«корпоративн» — он всегда перекрыт сильным «корпоратив» и не мог сработать ни
разу.

## Что проверено и оказалось верным

Не всё, что я проверяла, оказалось дефектом, и это стоит записать отдельно.

**Утверждения «не хватает слов» достоверны.** Восемь проверенных вручную по
файлам репозитория — «инструкция», «sites», «санкции», «vpn», «фреймер» для
страницы Framer, «гитлаб», «enterprise», «bronze» для страницы GitLab — дали
ноль вхождений в каждом случае. Прежняя починка сравнения словоформ
работает, и этой части отчёта можно верить.

**Режимы Threat и порог динамики согласованы.** «Полный режим требует 6
измерений» и «медианы по 3 измерениям» — не противоречие: три измерения на
каждое из двух окон.

**Отказ считать переходы обоснован.** Показы Вебмастера действительно не
объём рынка, и отчёт прав, что не переводит их в переходы.

## Приоритеты и последовательность

Порядок продиктован зависимостями, а не важностью по отдельности: правка 1
меняет состав пакетов, который читают правки 3 и 4, поэтому идёт первой.

### П1 — неверное действие. Одной веткой, до следующего прогона

| # | Что | Где | Объём |
| --- | --- | --- | --- |
| 1 | Занятые пакеты вывести из очереди списком, а не пометкой; в отчёт и письмо добавить раздел «Заблокировано чужим замером» с датой освобождения — по образцу «Проверить, а не делать» | `run_daily.py:304`, `deep_report.py`, `mailer/` | строка + раздел |
| 2 | Передать `core_stable` в `rank()` веб-отчёта | `deep_report.py:409` | один аргумент |

Обе — с тестом, который падает на прежнем поведении: пакет с занятостью не
должен появляться в списке поручений; `rank()` при `core_stable=False` не
должен отдавать полный режим. Без тестов правка вернётся: изъян 4 —
недочиненный остаток прежней такой же починки.

### П2 — неверное число перед руководителем. Той же веткой

3. «+0 переходов» → «не считается: спрос измерен показами Вебмастера»,
   когда ни у одного пакета нет `traffic_upside`.
4. Счётчик состояний: завести отдельные строки «снято с поручений» и «не
   поручение — проверка», убрать их из «вне плана»; согласовать с подписью
   «из них в плане работ».
5. Контрольные группы вынести из «в очереди» в свою строку.

### П3 — методика. Отдельной веткой, после П1–П2

6. Сверку с Вебмастером считать по срезу на конец окна Вебмастера
   (`baseline_queries` уже умеет выбирать такой срез), а не по сегодняшнему.
   В текст добавить регрессию к среднему как конкурирующее объяснение
   профиля по диапазонам — либо снять вывод «дело не только в рекламе» до
   тех пор, пока она не исключена.
8. Развести «поле 639» и «ядро 489»: в шапке и плитках назвать, по какому
   набору считается каждое число.

### П4 — незакрытая петля Google. Отдельной задачей

7. Завести проверку индексации в Google по списку из 216 запросов и
   поставить её первой из трёх гипотез; результат — в план работ, а не в
   текст раздела. Объём больше остальных пунктов вместе взятых, поэтому
   отдельно и после того, как план работ перестанет врать о доступности.

Плюс одной строкой в итоги дня: сколько пакетов и точек атаки заблокировано
чужими замерами и до какой даты.

### Что не трогать

Пункт 9 (единицы в строке экспериментов, соседство двух дельт в письме) —
переформулировка текста. Делать вместе с ближайшей правкой писем, отдельной
ветки не заводить.
