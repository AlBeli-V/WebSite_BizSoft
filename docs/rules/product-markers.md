# Плашки над заголовком карточки

Решение руководителя 13.09.2026. Правило глобальное: действует на все
карточки каталога — существующие и те, что появятся позже.

## Правило

Над заголовком карточки стоят ровно две плашки.

**Первая — вид позиции**, выделена цветом:

| Вид позиции | Плашка |
|---|---|
| Продукт, ПО, подписка, план | `Командный план` либо `Индивидуальный план` |
| Подписка, у которой деления на команду и одного нет | `Универсальный план` |
| Пополнение баланса, токены, номинал | `Универсальный продукт` |
| Плагин, аддон, дополнение к продукту | `Дополнение к продукту` |

**Вторая — одна основная категория.** Даже если позиция логически попадает
в несколько разделов, в плашке стоит одна. У пополнений и дополнений это
основная категория производителя (`catSeg` и `catLabel` в
`src/data/vendors.ts`): сама позиция — надстройка над его продуктом, и своя
категория у неё бывает служебной. Вендора нет в реестре — берётся категория
самой позиции.

## Откуда берётся вид позиции

Считает `cardComposition` (`src/lib/product-composition.ts`) по данным:
`product_type`, `parent_sku`, артикул. Пополнения и дополнения определяются
однозначно, и плашка у них выводится сама.

Тип плана подписки данными **не определяется**: поля в каталоге нет, а
эвристика по названию и описанию (`src/lib/plan-type.ts`) молчит на 277
позициях из 601. Догадка здесь запрещена — она уже дала «Командный план ·
VFX и моушн» на карточке Red Giant.

Порядок источников эвристики: суффикс артикула (`-ORG`, `-TEAMS`, `-IND`,
`-INDIVIDUALS`) → слова названия («Teams», «Business», «Enterprise»,
«Individual», «личная») → **явное называние типа плана в первом абзаце
описания**: «командная подписка», «индивидуальный план», «лицензия для
команд», «для одного пользователя». Отдельные слова описания —
«организации», «команда», «личный» — признаком не считаются: описание по
правилу `card-lede-facts.md` упоминает их в любом контексте («лицензия
принадлежит организации», «для команды с общими шаблонами — план
Business»), и после записи описаний 13.09.2026 такая проверка перевела
317 карточек в «Командный план» (ADAudit Plus, Password Manager Pro,
пакеты кредитов). Пишущий описание называет тип плана в первом абзаце
ровно так, как он должен отобразиться на карточке.

Молчание эвристики — не пустая плашка. Решение руководителя 13.09.2026:
подписка без подтверждённого типа плана показывает `Универсальный план`
(`PLAN_MARKER_UNIVERSAL`). Это Visual Studio Professional, Perforce Helix
Core, Photon Fusion CCU и подобные, где план один для всех. Плашку
`Универсальный продукт` не путать: там плана нет вовсе — пополнение,
токены, номинал.

## Спорная карточка размечается оператором

Поле `marker` в контенте вендора (`VendorCardMeta`) старше эвристики:

```json
"cards": { "MAXON-REDGIANT-TEAMS": { "marker": "team" } }
```

Значения: `team`, `individual`, `universal`, `addon`.

**При заведении нового вендора или товара:** если не очевидно, к какому виду
относится позиция и в какую категорию её класть, — вопрос оператору до
записи в каталог, а не догадка. Пока ответа нет, карточка выходит с
`Универсальный план`: плашка не врёт про командный или индивидуальный и не
оставляет заголовок без вида позиции. Ответ оператора закрепляется полем
`marker`, а не правкой названия.

## Чем закреплено

Шаблон `src/pages/product/[slug].astro`, проверка
`tests/product-card-conversion.test.ts`.
