/**
 * Общая подготовка тестов.
 *
 * Лимиты форм и кэш справочника ходят в общее хранилище (коллекция app_kv
 * в Directus). В тестах базы нет, поэтому подставляем хранилище в памяти —
 * и обязательно заново перед каждым тестом: иначе счётчик или закэшированная
 * карточка организации перетекали бы из теста в тест, и порядок запуска
 * начал бы влиять на результат.
 */
import { beforeEach } from 'vitest';
import { createMemoryStore, setSharedStore } from '../src/lib/shared-store';

beforeEach(() => {
  setSharedStore(createMemoryStore());
});
