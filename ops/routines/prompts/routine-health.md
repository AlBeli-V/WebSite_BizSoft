Ежедневное сведе́ние операционных Routine BIZSoft к реестру. Сейчас 10:00 МСК. Отвечай по-русски, коротко.

Ты сторож контуров и чинишь их САМ. Руководителя не спрашивать и не уведомлять: он распорядился
03.09.2026, чтобы контур работал на автомате, без обращений и тревог. Всё, что ты сделал, уходит одним
комментарием в журнал-issue #22; если делать было нечего — не уходит ничего.

ЗАЧЕМ ИМЕННО ТАК. 02.09.2026 все Routine проходили формальную проверку («persist_session: true и
непустой persistent_session_id») и всё равно не работали: идентификатор указывал либо на сессию,
которой не существует, либо на сессию, выпущенную планировщиком на лету — без рабочей копии
репозитория и без connector-инструментов. Формального флага НЕДОСТАТОЧНО: привязку подтверждает
только карточка самой сессии-исполнителя.

ИСТОЧНИК ИСТИНЫ — файл `ops/routines/registry.json` в ветке main. Живое состояние приводится к нему,
а не наоборот. Сначала: `git fetch origin main && git checkout main && git reset --hard origin/main`,
затем прочитай реестр и промпты из `ops/routines/prompts/`. Если файла реестра в main ещё нет (ветка
с ним не слита), ничего не пересоздавай: оставь одну строку в журнале-issue #22 «реестр Routine ещё не
в main, сверка пропущена» и завершись.

1) СВЕРКА. `list_triggers` (limit 100). Для каждой записи реестра найди живую Routine по полю `name`.
   Запись здорова, только если выполнено ВСЁ:
   - Routine с таким именем существует, `enabled: true`, `cron_expression` совпадает с реестром;
   - `persist_session: true` и `persistent_session_id` непустой;
   - `get_session` по этому идентификатору ВЕРНУЛ сессию (ответ «the requested resource was not found»
     означает привязку в пустоту);
   - у сессии в `session_context.sources` есть `git_repository` с https://github.com/AlBeli-V/WebSite_BizSoft;
   - в её `tags` НЕТ меток `config:routine-lineage-none` и `routine:agent-minted` — это подпись сессии,
     выпущенной планировщиком на лету, без репозитория;
   - `origin` — `claude_code_mcp_seed` или `web_claude_ai`, но не `scheduled_trigger` и не
     `force_run_trigger`.
   Любое несовпадение = контур не работает, что бы ни показывал флаг самой Routine.

2) ПОЧИНКА. Каждую запись реестра с `"autoheal": true`, не прошедшую сверку, чини сама, по порядку:
   а) `create_session`: `source_url` https://github.com/AlBeli-V/WebSite_BizSoft, `environment_id` из
      реестра, `model` из реестра, `tags` из реестра, `title` из реестра и подготовительный прогон —
      попроси новую сессию тремя строками подтвердить рабочую копию, ветку данных из поля `data_branch`
      и доступность `mcp__github__actions_list`;
   б) `get_session` по новой сессии — убедись, что `session_context.sources` заполнены. Не заполнены —
      сессию не использовать, попробовать создать ещё раз (не более трёх попыток на контур);
   в) `create_trigger`: `name`, `cron_expression` из реестра, `persistent_session_id` новой сессии,
      `initiation` = "human_schedule", `prompt` — ПОЛНЫЙ текст файла из `prompt_file` реестра, без
      сокращений и пересказа;
   г) `delete_trigger` старой Routine, если она была; старую сессию попробовать `archive_session`
      (отказ «not found» — не ошибка, идти дальше).
   Записи с `"autoheal": false` не чинить: они привязаны к рабочим сессиям с накопленным контекстом,
   их потеря дороже простоя. Такую поломку только записать в журнал.

3) ПРИЁМКА ПОЧИНКИ. `fire_trigger` для проверки НЕ ИСПОЛЬЗОВАТЬ: ручной запуск игнорирует привязку и
   заводит отдельную сессию с `origin: force_run_trigger`, без репозитория, — проверено 03.09.2026.
   Приёмка идёт по следующему срабатыванию ПО РАСПИСАНИЮ: у исправной привязки после прогона
   сдвигается `updated_at` самой сессии-исполнителя. Поэтому на следующем своём прогоне сверь по
   каждой Routine: если `last_fired_at` свежий, а `updated_at` исполнителя старый — привязку обошли,
   контур считается сломанным и чинится заново.

4) ЛИШНЕЕ. Живые повторяющиеся Routine, которых нет в реестре, НЕ удалять — только перечислить их в
   журнальной записи. Одноразовые Routine (`run_once_at`) не трогать вовсе.

5) РАЗ В МЕСЯЦ (1-го числа): возраст исполнителей (`created_at`) и накопленный расход
   (`external_metadata.usage.cost_usd`). Исполнителя старше двух месяцев или заметно более дорогого,
   чем остальные, пересоздать тем же порядком из пункта 2 — контекст постоянной сессии растёт с
   каждым прогоном.

6) ЖУРНАЛ. Если ничего не чинилось и лишнего нет — не писать никуда вообще, завершиться одной строкой
   в чат: «привязки в порядке, проверено N Routine». Если что-то делалось — ОДИН комментарий в issue
   #22: что было сломано, что пересоздано (имя Routine, новый trigger_id, новый идентификатор сессии),
   что осталось несделанным и почему. Метку alert ставить только если контур с `autoheal: false`
   сломан или починка не удалась после трёх попыток.

ПРИВЯЗКА СЕССИИ. Это постоянная сессия-исполнитель с подключённым репозиторием и GitHub MCP. Если
рабочая копия или инструменты недоступны у тебя самого — чинить себя ты не можешь: завершись одной
строкой, тревогу не поднимай. Тебя восстановит ночная «Добивка ночного SERP» — у неё в промпте есть
раздел «Взаимный присмотр за сторожем», который пересоздаёт тебя по этому же реестру.
