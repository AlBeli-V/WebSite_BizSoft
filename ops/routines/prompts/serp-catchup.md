Страховочная добивка ночного SERP-среза biz-soft.pro (поручение руководителя 01.09.2026: планировщик GitHub системно пропускает cron-слоты, критичные запуски дублируются Routines). Сейчас 02:45 МСК. Отвечай по-русски.

ГРАНИЦЫ. Это операционный прогон, не сессия разработки. Запрещено: коммитить в любые ветки, менять код, workflows, секреты; сливать PR. Разрешено только: чтение репозитория и веток данных, запуск workflow через GitHub MCP (actions_run_trigger, всегда ref main), проверка результатов, работа с Routine по разделу «Взаимный присмотр» ниже.

Задача:
1) git fetch origin seo-data и проверить, существует ли reports/seo/serp/<СЕГОДНЯ по МСК, UTC+3>-serp.jsonl в origin/seo-data с успешными строками (строки без error, с top).
2) Если файл есть и наполнен — ничего не запускать, перейти к разделу «Взаимный присмотр» и тихо завершиться (одна строка отчёта).
3) Если файла нет — проверить через GitHub MCP, не идёт ли уже run workflow seo-serp-watch.yml (status in_progress/queued). Если идёт — дождаться завершения и проверить результат. Если не идёт — запустить workflow seo-serp-watch.yml с ref main (workflow_dispatch без inputs), дождаться завершения (обычно 25–40 минут: отложенные запросы опрашиваются до дедлайна), затем снова проверить файл среза за сегодня.
4) Бюджет стережёт сам скрипт (дневной потолок 700 и месячный 20000 по журналу serp/ledger) — потолки не обходить, ничего в коде не менять.
5) Если после запуска сбор не удался или run упал — создать issue с меткой alert (симптом, ссылка на run, лог ошибки). Руководителю в чат не писать: метка alert и есть канал тревоги. Ночной тариф веб-поиска действует до 07:59 МСК — повторный запуск позже этого времени не делать, только issue.

ВЗАИМНЫЙ ПРИСМОТР ЗА СТОРОЖЕМ (выполнять каждый прогон, коротко). Ежедневный сторож «Проверка привязок операционных Routine» чинит привязки всех контуров, но себя починить не может: сломанная Routine не запускается. Поэтому раз в сутки его проверяет этот прогон.

- `list_triggers` (limit 100), найти Routine с именем, начинающимся на «Проверка привязок операционных Routine».
- Если её нет вовсе, либо `get_session` по её `persistent_session_id` не вернул сессию, либо у сессии нет `session_context.sources` с https://github.com/AlBeli-V/WebSite_BizSoft, либо в её `tags` есть `config:routine-lineage-none` или `routine:agent-minted` — сторож сломан, восстанови его по реестру: прочитай `ops/routines/registry.json` в main, возьми запись с ключом `routine-health` и выполни порядок из раздела «Порядок восстановления» файла `ops/routines/README.md`.
- Если сторож исправен — ничего не делай и не упоминай его в отчёте.
- Восстановление сторожа отметь одной строкой в журнале-issue #22. Руководителю не писать.

ПРИВЯЗКА СЕССИИ. Это постоянная сессия-исполнитель с подключённым репозиторием и GitHub MCP. Если
рабочая копия недоступна или инструменты GitHub не загружаются — руководителю НЕ писать и тревогу не
поднимать: контур чинится сам. Оставь одну строку в журнале-issue #22 вида «добивка SERP: привязка
сессии сломана, прогон пропущен» и тихо завершись. Сторож пересоздаст исполнителя по реестру
`ops/routines/registry.json`. Если недоступен и GitHub MCP (issue завести нечем) — просто завершись
одной строкой в чат.
