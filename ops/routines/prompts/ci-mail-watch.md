Страховочная проверка утреннего письма BIZSoft Competitive Intelligence. Сейчас 10:45 МСК. Отвечай по-русски.

Зачем эта проверка. У workflow competitive-intelligence-mail.yml есть собственный сторож по расписанию GitHub (cron 07:40 UTC), но планировщик GitHub в этом проекте системно пропускает cron-слоты: 01.09.2026 сторож не сработал вовсе, и о том, что письмо не ушло, руководитель узнал сам. Эта проверка дублирует сторожа независимым каналом.

ГРАНИЦЫ. Это операционная проверка, не сессия разработки. Запрещено: коммитить в любые ветки, менять код, workflows, секреты, Routines; сливать PR; запускать платные закупки SERP. Разрешено: чтение репозитория и веток данных, чтение прогонов, запуск почтового workflow (только ref main).

Задача (СЕГОДНЯ = дата по МСК, UTC+3):

1) Через GitHub MCP посмотри прогоны почтового workflow competitive-intelligence-mail.yml за сегодня (actions_list, method list_workflow_runs), затем шаги нужного прогона (method list_workflow_jobs). Письмо действительно ушло, только если выполнились шаги «Quality gate» и «Send letter or failure notice» — то есть режим send. Прогон со статусом success, но с пропущенным шагом Quality gate, означает режим skip: руководитель письма НЕ получил.

2) Если письмо за сегодня ушло — ничего не делай, тихо завершись одной строкой отчёта.

3) Если письма нет — выясни, где встала цепочка:
   - git fetch origin competitive-data; git ls-tree --full-tree --name-only origin/competitive-data reports/competitive/
     Есть ли reports/competitive/<СЕГОДНЯ>-email.html? Нет файла — прогон не состоялся или не запушил;
   - если файл есть, а прогона почтового workflow за сегодня нет — запусти его через GitHub MCP (actions_run_trigger, method run_workflow, workflow_id competitive-intelligence-mail.yml, ref main). Дождись завершения и проверь шаги, как в пункте 1;
   - если файла нет — посмотри, есть ли сегодняшний срез выдачи у базового контура:
     git fetch origin seo-data; git ls-tree --full-tree --name-only origin/seo-data reports/seo/serp/ и то же для reports/seo/data/serp/
     Каталог срезов уже переезжал 01.09.2026, и это ломало прогон конкурентной разведки.

4) Если после твоих действий письмо всё равно не ушло — заведи issue с меткой alert: что именно не сработало, на каком шаге, что ты предпринял, что осталось сделать человеку. Руководителю в чат не писать: метка alert и есть канал тревоги. Не чини код и workflows; проблему кода опиши словами в issue.

ПРИВЯЗКА СЕССИИ. Это постоянная сессия-исполнитель с подключённым репозиторием и GitHub MCP. Если
рабочая копия недоступна или инструменты GitHub не загружаются — руководителю НЕ писать и тревогу не
поднимать: контур чинится сам. Оставь одну строку в журнале-issue #22 вида «сторож письма: привязка
сессии сломана, прогон пропущен» и тихо завершись. Ежедневный сторож «Проверка привязок операционных
Routine» пересоздаст исполнителя по реестру `ops/routines/registry.json` и вернёт контур в строй. Если
недоступен и GitHub MCP (issue завести нечем) — просто завершись одной строкой в чат.
