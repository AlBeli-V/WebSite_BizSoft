Страховочная добивка Wordstat Intelligence biz-soft.pro (поручение руководителя 01.09.2026: планировщик GitHub системно пропускает cron-слоты, критичные запуски дублируются Routines). Сейчас 05:35 МСК. Отвечай по-русски.

ГРАНИЦЫ. Это операционный прогон, не сессия разработки. Запрещено: коммитить в любые ветки, менять код, workflows, секреты, Routines; сливать PR. Разрешено только: чтение репозитория и веток данных, запуск workflow через GitHub MCP (actions_run_trigger, всегда ref main), проверка результатов.

Задача (СЕГОДНЯ везде = дата по МСК, UTC+3):
1) git fetch origin seo-data. Проверить в origin/seo-data наличие reports/seo/wordstat/<СЕГОДНЯ>-full-result.json ИЛИ свежего <СЕГОДНЯ>-demand-report.md (признак, что ночной полный цикл прошёл).
2) Если ночной цикл не прошёл и run workflow seo-wordstat.yml сейчас не идёт — запустить seo-wordstat.yml с ref main, inputs {"mode":"full"}; дождаться завершения (~30–60 минут) и проверить появление файлов. Если run уже идёт — дождаться и проверить.
3) Понедельник: дополнительно проверить reports/seo/wordstat/momentum-<СЕГОДНЯ>.json; если нет — после шага 2 запустить seo-wordstat.yml с inputs {"mode":"momentum"} (прогоны сериализуются concurrency-группой, просто ждать), дождаться, проверить, что в файле items с непустыми series.
4) Первое число месяца: дополнительно проверить reports/seo/wordstat/region-demand-<ГГГГ-ММ>.json; если нет — запустить seo-wordstat.yml с inputs {"mode":"regions"}, дождаться, проверить непустые top_regions.
5) Если всё уже на месте — ничего не запускать, тихо завершиться (одна строка отчёта).
6) Бюджет и квоты стережёт сам конвейер (журнал wordstat/ledger, лимитер) — не обходить, код не менять. При падении run или пустых результатах после добивки — issue с меткой alert (симптом, ссылка на run, фрагмент лога). Руководителю в чат не писать: метка alert и есть канал тревоги.
7) Если прогон занял больше, чем ты готов ждать в одном заходе, поставь себе одноразовое напоминание через `send_later` на нужный момент и доведи проверку результата до конца — не оставляй прогон недоведённым.

ПРИВЯЗКА СЕССИИ. Это постоянная сессия-исполнитель с подключённым репозиторием и GitHub MCP. Если
рабочая копия недоступна или инструменты GitHub не загружаются — руководителю НЕ писать и тревогу не
поднимать: контур чинится сам. Оставь одну строку в журнале-issue #22 вида «добивка Wordstat: привязка
сессии сломана, прогон пропущен» и тихо завершись. Ежедневный сторож «Проверка привязок операционных
Routine» пересоздаст исполнителя по реестру `ops/routines/registry.json` и вернёт контур в строй. Если
недоступен и GitHub MCP (issue завести нечем) — просто завершись одной строкой в чат.
