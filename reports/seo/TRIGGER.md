# Восстановление ежедневного триггера отчётности (V4)

Система запускается Routine-триггером Claude Code (среда CCR, сессия «BIZSoft
Growth Intelligence»). Если триггер или сессия утеряны — создать заново из
любой сессии Claude Code этого репозитория.

- **Расписание:** cron `0 6 * * *` UTC (09:00 МСК, ежедневно; время проекта —
  московское, правило CLAUDE.md).
- **Текущий триггер:** `trig_013uQS6iK8LGVQETEaKDWFxY`, сессия
  `session_01JoQwrqUDpG5fpgeEWaNzKx`.
- **Устройство:** код — в `main`, машинные данные — в orphan-ветке `seo-data`
  (см. README этой ветки); обмен — `scripts/seo/data_sync.sh`.

Промпт хранится не в репозитории, а в настройке Routine. Этот файл — источник
истины для его текста: при расхождении настройка приводится к тексту отсюда.
Правится промпт в интерфейсе Claude Code (Routines → «BIZSoft Growth
Intelligence») — инструмент `update_trigger` не редактирует промпт Routine,
привязанного к чужой персистентной сессии.

Промпт триггера (актуальный текст, копировать целиком):

```
Ежедневный прогон BIZSoft Growth Intelligence (письмо V4 «Executive Command Center»). Методика — reports/seo/README.md и docs/seo/reporting-methodology.md. Код — в main, машинные данные — в orphan-ветке seo-data; обмен данными — только через scripts/seo/data_sync.sh.

ГРАНИЦЫ ПРОГОНА. Это операционный прогон, а не сессия разработки. В ходе прогона запрещено: коммитить в main; изменять любые файлы .github/workflows/** в любой ветке; изменять CLAUDE.md, package.json, pnpm-lock.yaml, конфигурацию сборки и деплоя; изменять содержимое scripts/** и src/**, кроме случая из пункта 7; коммитить в seo-data что-либо вне каталогов reports/seo/data, reports/seo/wordstat, reports/seo/intelligence, reports/seo/public; самостоятельно сливать pull request; менять настройки Routine; изменять или удалять ветки seo-data и archive/rating-tracking-5rf03g; менять secrets, цели аналитики, цены, домены, рекламные кабинеты.

Если в ходе прогона обнаружена проблема инфраструктуры, workflow, триггеров или кода — не исправляй её. Создай issue с меткой alert, опиши симптом, доказательство и предлагаемое решение, и продолжи прогон. Исправление — отдельная задача в отдельной ветке с моим участием. Формулировка «так будет быстрее» не является основанием для отступления от этих границ.

0) git fetch origin main seo-data; git checkout main; git pull; bash scripts/seo/data_sync.sh pull. Запусти воркфлоу seo-data-collect.yml и seo-site-check.yml (GitHub MCP actions_run_trigger, ref main), дождись успешного завершения и снова bash scripts/seo/data_sync.sh pull. Просмотри коммиты main за сутки.

1) Конвейер: python3 scripts/seo/snapshot.py <дата>; python3 scripts/seo/quality.py <дата>; python3 scripts/seo/report_v4.py <дата>; python3 scripts/seo/webreport.py <дата>; node scripts/seo/emailcheck.mjs reports/seo/intelligence/<дата>-v4.html reports/seo/intelligence/<дата>-v4-render.json; python3 scripts/seo/uxlint_v4.py <дата>; python3 scripts/seo/contentcheck.py <дата>; python3 scripts/seo/previews_v4.py <дата>; python3 -m unittest discover -s scripts/seo/tests -t scripts/seo/tests. UX lint, качество содержания и тесты обязаны быть зелёными — иначе письмо не отправляется, а причина фиксируется в отчёте и в issue.

2) Актуализируй reports/seo/intelligence/actions.json: статусы, стадии (proposed/approved/implemented/tested/deployed/observing/accepted/rejected/blocked), сроки, владельцев из карты ролей, поля status_changed_at, pr/ci/qa/deploy/rollback и demand_clusters. В журнал письма попадают только задачи со сменой статуса сегодня, блокировкой или сроком в ближайшие 7 дней. RED (бюджет, юридические обязательства, цены, домены, необратимые изменения) — блок «От вас»; если RED нет, письмо показывает «действий не требуется».

3) Правила письма: 800–1000 видимых слов, первый экран до 250; ≤4 показателя, ≤3 сигнала, ≤3 возможности, ≤5 строк журнала, ≤3 изображения. Причина изменения либо подтверждена перечисленными страницами и запросами, либо не называется. Нулевая дельта — «без изменений». Если источник не обновился (та же latest_event_date, что вчера) — сказать это прямо и не подавать вчерашнюю дельту как новость. Клики выборки и визиты сайта не сводятся к одному определению визита; CTR публикуется как CTR выборки с пометкой охвата; расхождение охватов — статус limited, а не degraded. Совместное внедрение — один эксперимент. «Рыночный спрос» разрешён только при замере Вордстата. Один факт не повторяется в письме больше двух раз.

4) Веб-отчёт reports/seo/public/daily/<дата>/index.html содержит полные таблицы запросов и страниц, карту измерений, историю экспериментов, журнал исполнения, качество данных и методику. Письмо ведёт туда; пока PUBLIC_REPORT_BASE_URL не задан, ссылка помечается как техническая копия.

5) bash scripts/seo/data_sync.sh push "Отчёт за <дата>" — push в seo-data запускает seo-report-email (письмо на avbelyaev@biz-soft.pro; одно письмо на дату — маркер reports/seo/intelligence/last-mailed.txt). Проверь статус run. Если письмо не ушло — не чини причину и не отправляй ручным dispatch вслепую: сначала убедись, что отчёт за сегодняшнюю дату существует, и только тогда запусти seo-report-email с явным указанием сегодняшней даты. Если причина неотправки в инфраструктуре — issue с меткой alert.

6) SendUserFile: preview письма (<дата>-v4.html, display render, status proactive) и веб-отчёт; в caption — итог дня одним предложением и что требуется от руководителя.

7) Если рыночный спрос собран полностью (market_demand.complete) и в нём есть разрывы — подготовь правки карточек товара по измеренным коммерческим фразам. Разрешено изменять только файлы карточек и лендингов товаров в src/. Оформи отдельной веткой от актуального main и pull request с указанием фразы и её частотности по каждой правке, тесты и сборка зелёные. PR НЕ сливай — его сливает руководитель. Полномочие на самостоятельный мерж, выданное 19.08.2026, отозвано.

Понедельник — недельный отчёт (weekly/), 1-е число — месячный (monthly/). Ограничения: рекламные кабинеты не трогать; цены, домены, юридические обязательства и бюджет — решение руководителя; изменения сайта — только через PR с обоснованием измеренным спросом и мержем руководителя.
```
