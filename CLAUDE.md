# Правила проекта biz-soft.pro

Короткий свод. Полный текст каждого правила, история и разборы инцидентов —
в `docs/rules/` (файл указан у каждого пункта); карта проекта — `docs/INDEX.md`.
Свод читается в каждой сессии, поэтому новое правило заводится здесь одной
строкой-двумя, а обоснование — отдельным файлом в `docs/rules/`.

## Как работать

- **Проверка подхода до начала работы** (`docs/rules/approach-check.md`).
  Открывая ветку или задачу, проверить: механизм (детерминированная работа —
  скрипт или GitHub Actions, сессия только там, где нужна оценка или текст),
  объём (правки одного контура за день — одна ветка и одна сессия, не серия
  микро-PR), модель (операции и правки по шаблону — Sonnet; проектирование,
  аудиты, разбор инцидентов — Opus/Fable), чтение (генерируемые данные не
  читать целиком, документы — по нужному разделу), готовое (искать
  существующий workflow или скрипт). Расхождение — сказать до начала работы
  и предложить путь с обоснованием из трёх частей. Контур ведёт одна сессия:
  вторая в том же контуре в тот же день не открывается — реестр очереди
  показывает состояние на момент merge, и параллельная работа в нём не видна
  (14.09.2026: две сессии написали одни и те же 66 карточек).
- **Язык** (`docs/rules/language.md`). Всё для руководителя — по-русски:
  ответы, вопросы, коммиты, PR, комментарии в коде, письма, отчёты.
  Внутренние рассуждения модели — на любом языке. Английский в результате —
  только технические идентификаторы, команды, цитаты внешних сервисов.
- **Решения — кнопками с визой** (`docs/rules/decisions-buttons.md`). Любой
  запрос решения руководителя — AskUserQuestion: 2–4 взаимоисключающих
  варианта, у каждого виза «РЕКОМЕНДУЮ» / «НЕ РЕКОМЕНДУЮ» и обоснование (чем
  подтверждается, что даёт, чем рискуем); рекомендуемый — первым, виза в
  описании варианта; кнопки «Другое» нет. Всё, что не зависит от ответа,
  делается до вопроса. Текстом без кнопок — только доклады.
- **Время — московское** (`docs/rules/timezone.md`). Время без пояса —
  МСК (UTC+3, без перехода). В cron и расписаниях — UTC с комментарием МСК.
- **Письма руководителю — на avbelyaev@biz-soft.pro** (`docs/rules/mail-recipient.md`)
  через workflow `ops-send-mail` (только с main). Почта профиля сессии —
  только для авторства коммитов.

## Прод и операции

- **Доступ к проду** (`docs/rules/prod-access.md`). Сетевой доступ из сессии
  закрыт: проверки и операции — через ops-* workflows, результаты — в
  issue #22. Деплой — только deploy.yml при пуше в main. Журнал прогона
  маскирует секреты: слово `deploy` в слаге выводится как `***` — такую
  строку в данные не заводить (`tests/no-masked-slugs.test.ts`).
- **Production-workflow — только с main** (`docs/rules/production-workflow-main-only.md`).
  Ручной запуск workflow, который деплоит, ходит по SSH, пишет в Directus,
  меняет аналитику или шлёт письма, — только с ветки main; Guard не
  обходить. В ops-fetch-assets и ops-me-crawl ветка назначения — рабочая.
- **Операционные прогоны — только GitHub Actions** (`docs/rules/operational-runs.md`).
  Ежедневные и еженедельные прогоны с детерминированными шагами — workflow с
  двумя cron-слотами, идемпотентностью по артефакту дня, очередью и явным
  вызовом почтовых workflow (`scripts/ops/gh_dispatch_wait.sh`). Routine на
  сессиях Claude для них не заводятся. Время старта задаёт сторож
  `seo-morning-gate`: планировщик GitHub задерживает слоты на 4–5 часов,
  поэтому утренние контуры запускает он, а их собственные слоты остаются
  страховкой. Контуры: `seo-daily-report`,
  `seo-committee-build`, `competitive-intelligence-daily`, `seo-serp-watch`,
  `seo-wordstat`. Статусы `intelligence/actions.json` обновляет PR,
  внедряющий действие. Тишина: при успехе ничего, при сбое — запись в issue #22.
- **Источники и аккаунты — в реестрах с переключателем** (`docs/rules/data-sources-registry.md`).
  Поставщик данных — запись в `data/seo/domain-data-sources.json` (функция →
  действующий источник → запасные), аккаунт сервиса — в `ops/accounts/registry.json`
  (без значений секретов, с группой владельцев и импактом). Смена поставщика —
  правка поля `active` через PR; источник из заблокированной группы действующим
  не назначается. Домены проекта сторожит `ops-domain-watch` по RDAP.
- **Достоверность отчётов** (`docs/rules/report-integrity.md`). В тексте
  отчётов, промптов и журнала нет литералов без срока годности (даты,
  обещания, суммы, квоты) — `test_report_literals`, исключения с
  `valid_until`. Недоступный блок письма — только `passport.unavailable`;
  ложь о дате или причине блокирует выпуск. Запись в issue #22 — только
  `journal-post` с `outcome`; `|| true` вокруг основного скрипта запрещён.
  Шаг по SSH — через `./.github/actions/ssh-run` (вывод доходит до журнала
  и при сбое), не `appleboy/ssh-action` с `capture_stdout` напрямую.
  Секреты — в `ops/secrets/registry.json`; одноразовые workflow с
  `# expires:`. Периодные сравнения — только по полным окнам.
- **Изменения каталога — под сторожем** (`docs/rules/catalog-watch.md`).
  Появление, исчезновение, снятие с витрины и возврат карточки, правка её
  цены, закупки, коэффициента, названия, слага или приписки — событие, о
  котором руководитель узнаёт письмом в тот же день (`ops-catalog-watch`,
  ежедневно). Исключение одно: переоценка по курсу ЦБ. После осознанной
  массовой правки базу сравнения пересобирают прогоном с `baseline=true`.
- **Товарные фиды закрыты** (`docs/rules/product-feeds.md`, `docs/yandex-feeds.md`).
  По умолчанию 404 (`X-Feed-Status: disabled`). Включать
  `YANDEX_FEEDS_ENABLED`, добавлять фиды в кабинеты, писать в поддержку
  площадок — только по прямой команде руководителя.

- **Визуальный слой отчётов — только KPI-kit** (`docs/rules/kpi-kit.md`).
  Плитки, светофор, графики и таблицы писем и веб-отчётов обоих контуров —
  компоненты `scripts/viz/kpi_kit.py`; свои цвета, сторонние библиотеки
  графиков и внешние сервисы рендера не заводятся. Серия = сущность (Яндекс
  и «мы» — синий, Google — оранжевый), статус — только с глифом, одна ось на
  график. Жёсткий размер внутри гибкого контейнера запрещён: пиксельная ширина —
  только вместе с правом сжаться, иначе блок прокручивается внутри себя.
  Проверка — `scripts/seo/tests/test_kpi_kit.py`, `uxlint_v4.py`, собранная
  страница — `scripts/seo/layoutcheck.mjs` (гейт трёх отчётных workflow).

- **Аудитория отчёта — устройства и каналы** (`docs/rules/audience-devices.md`).
  Письмо и веб-отчёт показывают показы Яндекса и Google по типам устройств и
  визиты по каналам (органика, реклама, внешние ссылки, каталоги, площадки) с
  той же разбивкой: плитки, под ними таблицы. Ряды — дневная витрина
  (`collect_daily.py`), окна только полные, «Яндекс» и «Гугл» в визитах — это
  Метрика и GA4, два счётчика одного сайта. Класс внешнего перехода — только из
  `data/seo/referral-classes.json` (каталог, площадка, форум); домена нет в
  реестре — «внешняя ссылка» и пометка «не размечен» в отчёте. В письме внешние
  переходы — одна строка с главными доменами, классы раскрываются от 20 визитов
  за неделю или 5 доменов; таблица доменов — в веб-отчёте.

- **Источник заявки — в письме, без домыслов** (`docs/rules/lead-source.md`).
  Письмо о заявке и о КП первой строкой называет тип трафика (органика,
  реклама, внешняя площадка, не определён), ниже — чем это подтверждается,
  фраза, кампания и объявление, цена клика, площадка и путь клиента.
  Неизвестное называется неизвестным со сроком, когда станет известно.
  Данные Метрики и Директа приходят утренним уточнением `ops-lead-source-mail`
  (режим `resend` — повтор писем задним числом); ПДн сервер не покидают.

- **Bitrix24 — зеркало, воронка в Directus** (`docs/rules/crm-mirror.md`).
  Заявка и первое скачивание КП копией уходят в портал (`src/lib/bitrix24.ts`),
  но источник воронки — Directus: по его стадиям считают конверсию, выручку и
  срок сделки. Обратно возвращается только стадия лида: исходящий вебхук
  портала → `/api/b24/hook` → карта `data/sales/b24-stages.json`; неописанная
  стадия заявку не меняет, а пишется событием в её историю. Контакты, текст и
  источник из портала не принимаются, сделки не зеркалятся. Зеркало не может
  уронить заявку (фоном, исход значением), код вебхука в лог не печатается,
  без секретов контур молча выключен. Секреты `B24_WEBHOOK_URL` и
  `B24_APP_TOKEN`, проверка и установка — `ops-b24-setup` (`check`, затем
  `apply`); связь заявки с лидом — поле `b24_lead_id`.

## Каталог, страницы, SEO

- **Поля форм с ПДн — класс `ym-disable-keys`** (`docs/rules/webvisor-masking.md`).
  Любое поле ввода персональных данных на публичной странице (ФИО, e-mail,
  телефон, компания, ИНН, текст обращения) несёт класс `ym-disable-keys`:
  Вебвизор не записывает ввод в нём. Проверка — `tests/webvisor-masking.test.ts`;
  настройки кабинета — `docs/marketing/webvisor.md`.
- **Каталог** (`docs/rules/catalog.md`). Directus — единственный источник
  товаров; вендор — точное поле `vendor` (сверять с `src/data/vendors.ts`).
  На bespoke-страницах вендоров не фильтровать по префиксу sku; ключи —
  slug или sku. Выгрузка каталога — `ops-export-products`. `lastmod` —
  `content_updated_at`. Задвоенные позиции склеивать `ops-merge-product`
  (сначала `apply=false`; 301 через `old_slugs`, снятая — `draft`);
  карточки не удалять.
- **Подарочные карты — варианты одного товара** (`docs/gift-cards.md`).
  `product_type = gift_card`: родитель со страницей и варианты-строки с
  `parent_sku`, регионом и номиналом; цена варианта = закупка USD × курс ЦБ
  × 3,0 (`GIFT_CARD_MARKUP_COEFF`), номинал в цене не участвует; страниц на
  номинал нет (301 на родителя, `productNoindex`), в списках витрины —
  только родитель, порядок номиналов — `denomination DESC` из кода.
- **Нижняя часть карточки — модули по классу товара** (`docs/rules/content-modules.md`).
  Поле `content_modules` собирается семействами из профиля вендора, проходит
  `scripts/content/validate-family.mjs` и пишется в прод только через
  `ops-apply-content`; пустое поле — legacy-рендер. Постановка и очередь —
  `docs/tasks/product-card-content-system/`.
- **Артикул — семь сегментов по единой системе** (`docs/rules/sku-system.md`).
  `<ВЕНДОР>-<ВИД>-<ПРОДУКТ>-<ПЛАН>-<СРОК>-<ЕДИНИЦА>[-<ВАРИАНТ>]`: коды
  вендоров — `data/catalog/sku-vendors.json`, продуктов —
  `data/catalog/sku-products.json`, словари — `src/lib/sku.ts`. Новая
  позиция без `sku` получает артикул из колонок `sku_*` при импорте; сайт
  читает вид, план, срок и noindex из сегментов (`parseSku`). Расстановка по
  каталогу — `data/catalog/sku-assignment.json` (генератор
  `scripts/catalog/sku-assign.mjs`, руками не править). Артикул — не слаг
  и не меняется; уникальность — в наборе сегментов, номера запрещены.
  Каталог переведён 13.09.2026, переходный слой снят: позиция без
  системного артикула — только черновик или архив (30 штук после разбора
  13.09.2026, перечень в журнале `ops-sku-migrate`), правило ей заводится
  в `sku-legacy-rules.json`.
- **Плашки карточки — вид позиции и одна категория** (`docs/rules/product-markers.md`).
  Первая плашка: `Командный план` / `Индивидуальный план` у подписки,
  `Универсальный план` — когда деления нет, `Универсальный продукт` у
  пополнения, `Дополнение к продукту` у плагина. Вторая — одна основная
  категория; у пополнений и дополнений берётся основная категория вендора.
  Вид считает `cardComposition`, тип плана данными не выводится: спорную
  карточку размечает оператор полем `marker` в контенте вендора, а при
  заведении новой позиции сомнение — это вопрос оператору, а не догадка.
- **Путь карточки — Главная → Производитель → Продукт** (`docs/rules/breadcrumbs.md`).
  Ровно три уровня, у всех товаров, существующих и новых. Разделов каталога и
  происхождения в пути нет — раздел остаётся маркером над заголовком и строкой
  параметров. Позиция без вендора получает «Каталог» вторым уровнем: это
  страховка от оборванного пути, а не композиция. Заводя вендора, запись в
  `src/data/vendors.ts` делают сразу — иначе в пути окажется сырое значение.
- **Строка производителя — знак, жирное имя, скрытая ссылка** (`docs/rules/vendor-line.md`).
  В карточке товара между словом «Производитель:» и названием стоит знак
  вендора единой высоты 20px (ширина — по пропорциям файла, длинная надпись
  вроде Zoho сдвигает название правее); название жирное; знак и название —
  одна ссылка на `/vendors/<slug>` без подчёркивания и цвета ссылки.
  Правило глобальное: все товары, существующие и новые.
- **Знак продукта — из пакета, иначе знак вендора** (`docs/rules/product-logos.md`).
  Знак семейства продукта берётся из `src/assets/product-icons`; позиция без
  него показывает знак вендора, плагины JetBrains — всегда знак JetBrains.
  Поиск знаков (Marketplace, Google) закрыт решением 13.09.2026; новый знак —
  только из официального файла вендора или по решению руководителя.
- **Заголовок карточки — имя продукта, ниже — план и срок** (`docs/rules/card-title.md`).
  H1 — название без маркеров плана и срока (`(Teams)`, `(личная)`, `1Y`,
  `(бессрочная)`, `для команд` уходят; редакция, тип места и «продление»
  остаются). Строка под ним: `Командный/Индивидуальный/Универсальный план ·
  срок`; срок — из названия и артикула, бессрочность — из описания,
  пополнение — «до истечения баланса», подписка без данных — «1 год»
  (умолчание, перечень на проверку — письмом; поправка — `src/data/card-terms.json`).
  Название в каталог заводится полным; ручные `term`/`termShort` сняты.
- **Описание и блок фактов карточки** (`docs/rules/card-lede-facts.md`).
  Описание — ~330 знаков видно, до трёх объёмов под «Подробнее»: назначение,
  состав плана, тип плана и его особенности, срок словами «1 год»; текст —
  в Directus через `product-descriptions.json` + `ops-apply-descriptions`.
  Под ним таблица из трёх равных колонок в одну строку по центру:
  СРОК / MIN / ТРАНСФЕР (на компьютере — полные заголовки), у каждого —
  значок «i» с пояснением; значения из данных: `1 год` / `∞` / `до истечения`,
  `minQty` (по умолчанию 1, пополнение — `50 $/€`), команда — `Да`, инди,
  пополнение и дополнение — `Нет`.
- **Блок цены — две ячейки, единица с минимумом, ступени 2/3/5/10/20** (`docs/rules/card-price-block.md`).
  Слева цена единицы без «от» и строка «в том числе НДС»; справа три
  пункта: `1 рабочее место (от N)` / `1 лицензия`, срок (`1 календарный
  год` / `бессрочно` / `до истечения`), тип плана. «Количество рабочих
  мест» жирным, ступени `qtyPresets` (ниже минимума нет; минимум 2 →
  3, 5, 10, 20; от 3 — с минимума), счётчик и ИТОГО открываются на
  минимуме; ступень ведёт счётчик, ИТОГО и подпись призыва со склонением.
  Под блоком цены ничего нет: ни приписки `price_note`, ни раскладки НДС.
  В колонке два действия одной формы — «Получить КП» во всю ширину и
  «Добавить в подборку» короче на флажок слева, подпись про единое КП по
  центру; ссылки «Задать вопрос» в колонке нет.
- **Новые страницы → sitemap → индексация** (`docs/rules/sitemap-indexing.md`).
  Проверить `/sitemap.xml` (bespoke-страницы — руками в `STATIC_ROUTES`;
  `noindex` и `productNoindex()` не попадают; `lastmod` = `content_updated_at`
  ставится применением сам). Переобход: основной канал — `ops-indexnow`
  адресно (`urls`, 10 000 в сутки, Яндекс + Bing); `ops-yandex-recrawl`
  (150 в сутки) — приоритетным страницам партиями в окно 00:30–00:40 МСК,
  до ночного `seo-recrawl-sweep`. Отчёты — в issue #22.
- **Микроразметка** (`docs/rules/structured-data.md`). Только штатный слой
  (`src/lib/seo.ts`, Breadcrumbs/FAQ/JsonLd, microdata карточки); один тип —
  один раз на страницу; один идентификатор (`@id` JSON-LD, `itemid`
  microdata) — один узел на странице (слои не связываются общим id, иначе
  поля дублируются); «цена по запросу» — без Product/Offer; фиктивные
  цены и рейтинги запрещены; цена — из `effectivePrice()`. Приёмка —
  `pnpm verify`.
- **WebMCP** (`docs/rules/webmcp.md`, `docs/webmcp/`). Новые товары и вендоры
  видны generic-инструментам автоматически; product-specific инструменты,
  параллельная база цен и write-инструменты запрещены; вызовы не шлют цели
  Метрики/GA4; `/api/agent/*` — noindex и вне sitemap. Изменил слой —
  `pnpm verify` и `pnpm test:webmcp-browser`.
- **Уникальные title и description** (`docs/rules/unique-meta.md`).
  `meta_title` ≤ 60 и `meta_description` ≤ 160 уникальны; различитель
  (период, объём, редакция, модель лицензии) — в `name` и заголовке.
  Правка меты в проде — только `data/seo/product-descriptions.json` +
  `ops-apply-descriptions` (`apply=false`, потом `apply=true`), затем
  переобход. Общий `short_description` разводится
  `scripts/seo/dedupe-descriptions.mjs`. Проверки: `pnpm test`
  (catalog-uniqueness), `pnpm smoke`, `ops-content-audit`.
- **Сниппет-эксперименты — только на страницах с показами**
  (`docs/rules/snippet-experiments.md`). Целевые фразы — из запросов
  Вебмастера; в реестре обязательны `page_markers`, `query_intent_any`,
  `query_exclude`, `title_marker`, `baseline`; статус `planned`, `start`
  ставит `seo-site-check`; одно изменение на эксперимент; после деплоя —
  переобход. Новый эксперимент любого типа — только на кластере с
  экспозицией выше 3,6 показа в день (100 за 28 дней): проверка
  `experiment_windows.py validate` перед отправкой реестра. Кластер под
  действующим опытом радар возможностей к правке не предлагает — называет тикет.
- **SERP: один сбор — все потребители** (`docs/rules/serp-single-source.md`).
  Выдачу собирает только `seo-serp-watch` (Яндекс ежедневно, Google RU через
  xmlriver еженедельно) в `reports/seo/serp` ветки `seo-data`; параллельные
  сборщики и прямые вызовы из потребителей запрещены; без свежего среза —
  «нет данных». Потолки и расписание — `data/seo/xmlriver.json` через PR.
- **Решение по кандидату — в машинный реестр** (`docs/rules/vendor-decisions-registry.md`).
  Отказ или одобрение вендора действует только записью в
  `reports/seo/wordstat/decisions.json` и
  `reports/seo/intelligence/vendor-decisions.json` (ветка `seo-data`; поля
  `brand`, `decided_on`, `reason`, `by`).
