# Icon corrections

Corrected from user-provided references: BrowserStack, CapCut, Kling AI, Leonardo AI, Principle.

Hierarchy correction: **Zoho = vendor**, **ManageEngine = product/product family**.

- Vendor UI: use `zoho-vendor-*`.
- ManageEngine product/SKU UI: use `manageengine-product-*`.
- All other corrected vendors: vendor cards use `*-vendor-*`; their product SKUs use the matching shared `*-product-*` mark.
- SVG canvas: 512×512 viewBox; artwork constrained to 12.5% safe zone.
- Color and monochrome variants supplied at 512/256/128 declared sizes.
- User-provided raster references are embedded in SVG; no automatic tracing.
