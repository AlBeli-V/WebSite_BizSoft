/**
 * Выключатель нового раздела Zoho ManageEngine.
 *
 * Раздел большой и трогает витрину производителя, поэтому у него есть
 * отдельный рубильник: если результат не устроит, откат не требует ни правки
 * кода, ни отката коммитов. Достаточно задать на сервере
 *
 *     PUBLIC_ZOHO_HUB=0
 *
 * и пересобрать — `/vendors/zoho` вернётся к прежнему шаблону лендинга, а
 * вложенные адреса `/vendors/zoho/*` будут отдавать 302 на него. Карточки
 * товаров при этом не трогаются: они живут в Directus и остаются на своих
 * адресах в любом положении рубильника.
 *
 * Значение по умолчанию — включено: переменная задаётся только тогда, когда
 * раздел нужно погасить.
 */
export const ZOHO_HUB_ENABLED = import.meta.env.PUBLIC_ZOHO_HUB !== '0';
