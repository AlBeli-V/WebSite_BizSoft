/**
 * Производители, которых спрашивают чаще всего.
 *
 * Список один на весь сайт: мега-меню шапки и страница /vendors обязаны
 * показывать одно и то же. До 11.09.2026 списков было два — восемь слагов в
 * `Header.astro` и двенадцать в шаблоне страницы, — и они разошлись: общими
 * были семь. Посетитель, пришедший из меню, видел на странице другой «топ»,
 * а редактор правил один список, не зная про второй.
 *
 * Порядок значим: он же задаёт порядок карточек и пунктов меню. Правится
 * руками — это редакторское решение, а не расчёт: измеренный спрос
 * (`vendor-demand.json`) выводит вперёд Google и Docker, которые нашим
 * покупателем спрашиваются не первыми.
 */
export const TOP_VENDORS: string[] = [
  'openai', 'anthropic', 'adobe', 'figma', 'jetbrains', 'microsoft', 'github', 'cursor',
];

/** Быстрая проверка «этот вендор в топе». */
export const isTopVendor = (slug: string): boolean => TOP_VENDORS.includes(slug);
