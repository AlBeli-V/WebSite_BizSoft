---
export const prerender = false;
// Старый адрес: он сохранён у руководителя на экране «Домой» айфона.
// Ломать закладку нельзя, поэтому переадресуем в кабинет.
return Astro.redirect('/admin', 308);
---
